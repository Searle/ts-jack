import Array from "./Array.jack";
import Math from "./Math2.jack";
import Memory from "./Memory.jack";
import Output from "./Output2.jack";
import Screen from "./Screen2.jack";
import String from "./String.jack";
import Sys from "./Sys.jack";
import Keyboard from "./Keyboard.jack";

export const os = () => {
    // return Array + Math + Memory + Output + Screen + String + Sys + Keyboard;
    return Array + Math + Memory + Output + Screen + String + Sys;
};
